{
    "rewrites": [{ "source": "/api/(.*)", "destination": "/api/notion" }],
    "headers": [
        {
            "source": "/(.*)",
            "headers": [
                { "key": "Access-Control-Allow-Origin", "value": "*" }
            ]
        }
    ]
}